import { RouteShellLoading } from "@/components/RouteShellLoading";

export default function Loading() {
  return (
    <RouteShellLoading
      eyebrow="Meta-Pet"
      title="Opening the next route"
      detail="The shared chrome stays light here so each destination can arrive with its own identity instead of the same fullscreen ritual."
    />
  );
}
