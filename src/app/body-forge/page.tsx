import { BodyForge } from '@/components/body-forge/BodyForge';

export const metadata = {
  title: 'B$S Body Forge',
  description: 'Body-only Meta Pet visual workshop.',
};

export default function BodyForgePage() {
  return <BodyForge />;
}
